# Worker — node-cron tabanlı ajan/Hermes zamanlayıcı. tsx ile TS'i doğrudan çalıştırır.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
ENV NODE_ENV=production
CMD ["node", "--import", "tsx", "src/worker/index.ts"]
